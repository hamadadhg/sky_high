/*
import 'package:flutter/material.dart';
import 'package:sky_high/core/styles/style_to_colors.dart';

class CustomIconError extends StatelessWidget {
  const CustomIconError({super.key, required this.percentToIconError});
  final double percentToIconError;
  @override
  Widget build(BuildContext context) {
    double height = MediaQuery.of(context).size.height;
    return Icon(
      Icons.error,
      color: StyleToColors.redColor,
      size: height * percentToIconError,
    );
  }
}
*/

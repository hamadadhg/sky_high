/*
import 'package:flutter/material.dart';
import 'package:sky_high/features/weather/presentation/views/widgets/custom_image_shimmer.dart';
import 'package:sky_high/features/weather/presentation/views/widgets/custom_text_shimmer_weather.dart';

class CustomInformationAboutWeatherShimmerSection extends StatelessWidget {
  const CustomInformationAboutWeatherShimmerSection({super.key});

  @override
  Widget build(BuildContext context) {
    double height = MediaQuery.of(context).size.height;
    return Row(
      mainAxisAlignment: MainAxisAlignment.spaceBetween,
      children: [
        Column(
          children: [
            const CustomTextShimmerWeather(),
            SizedBox(height: height * 0.05),
            const CustomTextShimmerWeather(),
            SizedBox(height: height * 0.02),
            const CustomTextShimmerWeather(),
            SizedBox(height: height * 0.02),
            const CustomTextShimmerWeather(),
            SizedBox(height: height * 0.02),
            const CustomTextShimmerWeather(),
          ],
        ),
        const CustomImageShimmer(),
      ],
    );
  }
}
*/

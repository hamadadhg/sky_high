/*
import 'package:flutter/material.dart';
import 'package:sky_high/core/styles/style_to_texts.dart';

class CustomTextToFourDetailsWeather extends StatelessWidget {
  const CustomTextToFourDetailsWeather({super.key, required this.text});
  final String text;
  @override
  Widget build(BuildContext context) {
    double height = MediaQuery.of(context).size.height;
    return Text(
      text,
      style: StyleToTexts.textStyle13.copyWith(fontSize: height * 0.018),
    );
  }
}
*/

/*
import 'package:go_router/go_router.dart';
import 'package:sky_high/core/constant/string_variables_constant.dart';
import 'package:sky_high/features/animation/presentation/views/animation_view.dart';
import 'package:sky_high/features/search/presentation/views/search_view.dart';
import 'package:sky_high/features/weather/presentation/views/weather_view.dart';

class GoRouters {
  static final GoRouter goRouter = GoRouter(
    routes: [
      GoRoute(
        path: kAnimationViewRouter,
        builder: (context, state) => const AnimationView(),
      ),
      GoRoute(
        path: kSearchViewRouter,
        builder: (context, state) => const SearchView(),
      ),
      GoRoute(
        path: kWeatherViewRouter,
        builder: (context, state) => const WeatherView(),
      ),
    ],
  );
}
*/
